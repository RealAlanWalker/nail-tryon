# MediaPipe vendor files

Place the local browser runtime files from `@mediapipe/tasks-vision` here.

Required at minimum:

- `vision_bundle.mjs`
- `vision_wasm_internal.js`
- `vision_wasm_internal.wasm`

If your package version includes additional wasm/js assets, keep them in this folder too.
